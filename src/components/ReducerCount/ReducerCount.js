import React from 'react';

const ReducerCount = (props) => {
    return (
        <div>
            <h1>This is Reducer Count : {props.count}</h1>
        </div>
    );
};

export default ReducerCount;